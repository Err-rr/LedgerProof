# Server-only -- never NEXT_PUBLIC_. The browser never talks to this URL
# directly; it goes through this app's own route handlers (app/api/**).
#
# Local development against the real api/ app with in-memory persistence
# (no Neon/S3/Anthropic credentials needed):
#   python scripts/dev_api_server.py --port 8000
LEDGERPROOF_API_BASE_URL=http://127.0.0.1:8000
